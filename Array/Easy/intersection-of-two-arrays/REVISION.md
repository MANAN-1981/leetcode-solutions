# Quick Revision

## Pattern Used

HashTable

## Key Idea

The key insight is to use a hash table to store the elements of the first array, allowing for efficient lookups when iterating over the second array. This approach enables us to find the intersection in linear time complexity.

## Important Observations

Critical observations include: the importance of handling edge cases, the need to use a hash table to store the elements of the first array, and the requirement to check for duplicates in the intersection. For example, if we don't handle edge cases correctly, the function may return incorrect results.

## Formula

N/A

## Interview Tips

When explaining this problem in an interview, be sure to highlight the use of a hash table to store the elements of the first array, and the importance of handling edge cases correctly. Also, be prepared to discuss alternative approaches and their trade-offs.

---

## Revision Questions

**Q1.** What is the time complexity of the given solution?

**Q2.** How does the solution handle edge cases?

**Q3.** What are the trade-offs of using a sorting-based approach instead of a hash table-based approach?

**Q4.** How would you optimize the solution for very large input arrays?

**Q5.** What are the implications of using a hash table with a poor hash function?

**Q6.** How would you test the solution to ensure it is correct?

**Q7.** What are the advantages and disadvantages of using a binary search-based approach?

**Q8.** How would you modify the solution to find the union of the two input arrays instead of the intersection?

